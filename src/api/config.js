/**
 * API configuration — single source of truth for backend base URL.
 * Update this value to switch between dev/staging/production environments.
 */
export const API_BASE_URL = 'https://elctrofixers-client-side.vercel.app';
